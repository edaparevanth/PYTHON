r=int(input("Enter the rows:"))
c=int(input("Enter the columns :"))
mat=[]
for i in range(r):
    a=[]
    for j in range(c):
        a.append(int(input()))
    mat.append(a)

for i in range(r):
    for j in range(c):
        print(mat[i][j],end=" ")
    print()
